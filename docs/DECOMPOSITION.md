# Декомпозиция работ (WBS): виджет «Trends»

Основано на [BRD.md](BRD.md). Статусы обновляются по ходу реализации.

## Этап 0 — Инфраструктура проекта
- [x] 0.1 Инициализация git-репозитория, `.gitignore`
- [x] 0.2 `project.yml` (XcodeGen): таргеты App, Widget-extension, Tests
- [x] 0.3 Локальный Swift-пакет `TrendsKit` (модели, сеть, парсинг — общий код)
- [x] 0.4 `Makefile`: generate / build / test / dmg
- [x] 0.5 Конфигурация SwiftLint

## Этап 1 — Ядро данных (TrendsKit)
- [x] 1.1 Модель `TrendItem` (запрос, трафик, ссылка, время, регион)
- [x] 1.2 `TrendsFeedParser` — парсер RSS (XMLParser, namespace `ht:`)
- [x] 1.3 `GoogleTrendsClient` — загрузка фида по гео (URLSession, HTTPS)
- [x] 1.4 `WorldAggregator` — агрегация US/GB/DE/BR/IN/JP: дедуп, сортировка по трафику
- [x] 1.5 `TrendsCache` — кэш последнего успешного ответа (UserDefaults/файл) с меткой времени
- [x] 1.6 Юнит-тесты: парсер (фикстура реального RSS), агрегатор, разбор трафика «500+»

## Этап 2 — Виджет (WidgetKit)
- [x] 2.1 `AppIntent`-конфигурация: параметр «Регион» (Россия / Мир)
- [x] 2.2 `TimelineProvider`: загрузка → кэш-фолбэк → таймлайн с обновлением ~30 мин
- [x] 2.3 SwiftUI-вью: small (топ-3), medium (топ-5), large (топ-10), светлая/тёмная тема
- [x] 2.4 Состояния «нет данных» и «данные из кэша от HH:mm»
- [x] 2.5 Deep-link: клик по строке → страница тренда в браузере

## Этап 3 — Приложение-контейнер
- [x] 3.1 Окно с предпросмотром трендов (Россия/Мир) и инструкцией по добавлению виджета
- [x] 3.2 Иконка приложения

## Этап 4 — Безопасность и конфигурация
- [x] 4.1 Entitlements: App Sandbox + `network.client` (минимум привилегий) для app и extension
- [x] 4.2 Info.plist: ATS по умолчанию (только HTTPS), категория, версия
- [x] 4.3 Проверка: нет секретов в коде, нет сторонних runtime-зависимостей

## Этап 5 — CI/CD
- [x] 5.1 `ci.yml`: xcodegen → сборка → юнит-тесты → SwiftLint (push/PR)
- [x] 5.2 `release.yml`: по тегу `v*` — Release-сборка, DMG (hdiutil), GitHub Release
- [x] 5.3 README: установка из DMG (снятие карантина), сборка из исходников

## Этап 6 — Приёмка
- [x] 6.1 Локальная сборка Release, установка в /Applications, добавление виджета
- [x] 6.2 Прогон критериев приёмки из BRD §5
- [ ] 6.3 Тег версии → публикация релиза (ждёт создания GitHub-репозитория)

## Этап 7 — v1.1: конфигурируемость и Топ/Райзинг (запрос заказчика 2026-07-23)
- [x] 7.1 `TrendingAPIClient` + парсер ответа batchexecute (объём, рост, время старта)
- [x] 7.2 Регионы: Россия / США / Европа (DE FR IT ES PL NL) / Мир (US GB DE FR BR IN JP RU)
- [x] 7.3 Окна времени: час / день / неделя (месяц недоступен в источнике — решение в BRD)
- [x] 7.4 Разделы: Топ (по объёму) и Райзинг (по % роста)
- [x] 7.5 Настраиваемый интервал обновления виджета: 15 / 30 / 60 мин
- [x] 7.6 Багфикс: мгновенное обновление при смене настроек (cache-first + фоновая дозагрузка)
- [x] 7.7 RSS-фолбэк при недоступности API; тесты нового парсера/сортировок
- [x] 7.8 Обновление приложения-контейнера (пикеры окна/раздела), README, повторная приёмка

## Этап 8 — v1.2: фикс обновления UI, иконка галереи, редизайн (запрос заказчика 2026-07-23)
- [x] 8.1 Тройная защита от «залипания» UI: грейс-таймаут сети 5 с в провайдере,
      интерактивная кнопка «Обновить» в виджете (AppIntent), принудительный
      reloadAllTimelines из приложения после загрузки данных
- [x] 8.2 Иконка в галерее виджетов: asset-каталоги AppIcon для app и extension
      (CFBundleIconName в обоих бандлах)
- [x] 8.3 Редизайн: градиентная шапка (синяя для «Топ», оранжевая для «Райзинг»),
      ранговые бейджи (топ-3 залиты градиентом), капсульные чипы метрик,
      закруглённая типографика, тонированный фон

## Этап 9 — v1.3: страны вместо регионов, дропдауны, единая таблица (запрос 2026-07-23)
- [x] 9.1 Диагностика бага обновления: виджет-«зомби» — размещённый виджет привязан
      к UUID старого расширения после переустановки; chronod не запускал новое.
      Фикс: перезапуск chronod в `make install` (перепривязка подтверждена логами)
- [x] 9.2 `TrendsCountry`: настраиваемый список из 30 стран (флаг + русское название
      из системной локали), модель `TrendItem.geo` для колонки «Страна»
- [x] 9.3 Виджет: мультивыбор стран через AppEntity («добавить из списка» в
      «Редактировать виджет»), флаги в шапке и в строках при нескольких странах
- [x] 9.4 Приложение: все параметры через dropdown (страны с чекмарками, раздел,
      период) + единая таблица (№, страна, запрос, объём, рост), выбор сохраняется
- [x] 9.5 Фикс обрезания medium-виджета: 4 строки, компактная шапка и отступы,
      выравнивание top
- [x] 9.6 Тесты обновлены (23, зелёные), живой смоук RU+US+TR — единая таблица с гео

## Этап 10 — документация (запрос 2026-07-23)
- [x] 10.1 docs/ARCHITECTURE.md: полный проход по цепочке вызовов всех файлов
      (3 сценария с sequence-диаграммой) + обоснование каждого алгоритма
- [x] 10.2 README в новом оформлении: логотип, SVG-превью виджетов, бейджи,
      быстрая установка, ASCII-схема архитектуры, таблицы возможностей и CI/CD
- [x] 10.3 Ассеты: docs/assets/logo.png, docs/assets/widget-preview.svg

## Этап 11 — v1.3.1: фикс применения настроек виджета (баг-репорт 2026-07-23)
- [x] 11.1 Диагностика по системным логам: ad-hoc подпись без Team ID →
      linkd не строит bundleIdentity → AppIntents декодирует все параметры
      конфигурации в nil (страны-AppEntity и AppEnum-«linkEnumeration») →
      виджет всегда рендерит дефолты RU/день/топ. Предыдущие фиксы (7.6, 8.1,
      9.1) чинили доставку таймлайна, а конфигурация терялась раньше
- [x] 11.2 project.yml: автоподпись Personal Team (`CODE_SIGN_STYLE:
      Automatic` + `DEVELOPMENT_TEAM`), `-allowProvisioningUpdates` в make
- [x] 11.3 Проверка по логам: EntityIdentifier-ошибки исчезли, параметры
      резолвятся; нюанс — старый процесс расширения переживает `killall
      chronod`, убивать и TrendsWidgetExtension
- [x] 11.4 Дистрибуция пересмотрена: DMG/release.yml удалены (ad-hoc
      артефакт бесполезен), CI сокращён до тестов и линта, README —
      единственный путь установки из исходников с указанием своего Team ID

## Этап 12 — v1.4: локализация (запрос 2026-07-23)
- [x] 12.1 Английский — язык разработки и дефолт (`developmentLanguage: en`),
      русский — перевод; String Catalogs (Localizable.xcstrings) в App,
      Widget и TrendsKit (`defaultLocalization: en` + ресурсы пакета)
- [x] 12.2 Все строковые литералы в коде переведены на английский;
      `TrendsCountry.name` — из текущей локали (было жёстко ru_RU);
      язык API-запроса `hl` — из локали (было жёстко «ru»)
- [x] 12.3 Тест названия страны стал локале-независимым; 23 теста и
      SwiftLint зелёные; ru.lproj проверены во всех четырёх бандлах
